{
  "name": "codec-validator-action",
  "version": "1.0.0",
  "description": "GitHub Action to validate codec.json files",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "package": "npm run build",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": [
    "github-action",
    "codec",
    "validation",
    "bacnet"
  ],
  "author": "Your Organization",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
